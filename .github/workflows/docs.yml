name: Auto Docs Update (Disabled)

on:
  workflow_dispatch   # Only runs if you manually click Run

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Auto README update is disabled
        run: echo "README auto update is now stopped."