name: Docker CI

on: [push]

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Simulate Docker Build
        run: |
          echo "Docker environment ready"
          echo "Image build simulated successfully"
